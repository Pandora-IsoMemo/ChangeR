# Check formula syntax

Check if the formula syntax is valid

## Usage

``` r
validateFormula(formula)
```

## Arguments

- formula:

  A formula to check

## Value

A formula if valid, otherwise an error
