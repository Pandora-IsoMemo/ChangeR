# Compare models using heuristic

The heuristic method is a model comparison method that combines the
number of parameters and the elpd_diff value.

## Usage

``` r
compareWithHeuristic(fit)
```

## Arguments

- fit:

  A list of mcp model fits

## Value

A loo model comparison object
