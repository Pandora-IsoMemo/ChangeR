# Clean the comb matrix

Check each row for '*+*' and if found replace that cell and all
following cells in the row with "". Remove Blank Rows. Remove duplicate
rows.

## Usage

``` r
cleanComb(comb)
```

## Arguments

- comb:

  A matrix containing the segmented formulas and priors

## Value

A cleaned matrix
