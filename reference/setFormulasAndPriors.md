# Set formulas and priors

Set formulas and priors

## Usage

``` r
setFormulasAndPriors(splittedComb)
```

## Arguments

- splittedComb:

  A list of two matrices containing the segmented formulas and priors

## Value

A list of lists containing the segmented formulas and priors
